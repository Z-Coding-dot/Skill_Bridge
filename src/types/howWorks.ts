import type { LucideIcon } from 'lucide-react';

export interface HowItsWorksProps {
    title: string;
    description: string;
    icon: LucideIcon;
    color?: string;
    iconBg?: string;
    opportunities?: string;
}
